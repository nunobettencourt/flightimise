import React from 'react';
import {useDispatch} from 'react-redux';
import * as actions from '../../store/flights/actions';

const FlightSearch = () => {
    const dispatch = useDispatch();

    return (
        <div className="flight-search">

        </div>
    )
}

export default FlightSearch;
